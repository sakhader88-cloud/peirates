# peirates
{     "name": "periates",     "image": "ghcr.io/devsecfranklin/peirates-dev",      // Use this environment variable if you need to bind mount your local source code into a new container.     "remoteEnv": {             "LOCAL_WORKSPACE_FOLDER": "${localWorkspaceFolder}"     } 
